services:
  weather-mcp:
    build:
      context: .
    container_name: weather-mcp
    command: ["weather-mcp", "--transport", "streamable-http", "--host", "0.0.0.0", "--port", "8765"]
    ports:
      - "8765:8765"
    restart: unless-stopped
